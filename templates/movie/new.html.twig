{% extends 'base.html.twig' %}

{% block title 'New movie !' %}

{% block main %}
    <div class="container">
        <main>
            <div class="row g-5 py-5">
                <div class="col-md-7 col-lg-8">
                    <h4 class="mb-3">Add a movie</h4>
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <hr class="my-4" />
                    <button class="w-100 btn btn-primary btn-lg" type="submit">Add movie</button>
                    {{ form_end(form) }}
                </div>
            </div>
        </main>
    </div>
{% endblock main %}
